apiVersion: v1
kind: Service
metadata:
  name: superset
  namespace: lakehouse
  labels:
    app: superset
spec:
  type: NodePort
  selector:
    app: superset
  ports:
    - port: 8088
      targetPort: web
      nodePort: 30088
      name: web
