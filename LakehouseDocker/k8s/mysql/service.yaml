apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: lakehouse
  labels:
    app: mysql
spec:
  type: NodePort
  ports:
    - port: 3306
      targetPort: mysql
      name: mysql
      nodePort: 30306
  selector:
    app: mysql
