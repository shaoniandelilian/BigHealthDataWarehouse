apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-metastore
  namespace: lakehouse
  labels:
    app: hive-metastore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hive-metastore
  template:
    metadata:
      labels:
        app: hive-metastore
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.36
          command:
            - /bin/sh
            - -c
            - |
              echo "Waiting for MySQL to be ready..."
              while \! nc -z mysql 3306; do
                sleep 3
              done
              echo "MySQL is ready."
        - name: download-mysql-driver
          image: curlimages/curl:8.5.0
          command:
            - /bin/sh
            - -c
            - |
              curl -fL --retry 3 --retry-delay 5 -o /driver/mysql-connector-j-8.0.33.jar \
                https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/8.0.33/mysql-connector-j-8.0.33.jar && \
              echo "MySQL driver downloaded."
          volumeMounts:
            - name: mysql-driver
              mountPath: /driver
      containers:
        - name: metastore
          image: apache/hive:standalone-metastore-4.2.0
          env:
            - name: SERVICE_NAME
              value: metastore
            - name: DB_DRIVER
              value: mysql
            - name: SERVICE_OPTS
              value: >-
                -Djavax.jdo.option.ConnectionDriverName=com.mysql.cj.jdbc.Driver
                -Djavax.jdo.option.ConnectionURL=jdbc:mysql://mysql:3306/hive_metastore?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
                -Djavax.jdo.option.ConnectionUserName=hive
                -Djavax.jdo.option.ConnectionPassword=hive123
            - name: IS_RESUME
              value: "false"
          ports:
            - containerPort: 9083
              name: thrift
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          volumeMounts:
            - name: hive-site
              mountPath: /opt/hive/conf/hive-site.xml
              subPath: hive-site.xml
            - name: mysql-driver
              mountPath: /opt/hive/lib/mysql-connector-j-8.0.33.jar
              subPath: mysql-connector-j-8.0.33.jar
          readinessProbe:
            tcpSocket:
              port: 9083
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 9083
            initialDelaySeconds: 120
            periodSeconds: 20
      volumes:
        - name: hive-site
          configMap:
            name: hive-metastore-config
            items:
              - key: hive-site.xml
                path: hive-site.xml
        - name: mysql-driver
          emptyDir: {}
